@model MvcBankingApplication.Models.ViewModels.AdminUserCreationModel

@{
  ViewData["Title"] = "Create Cashier";
}

<div class="bg-dark">
  <div class="text-white container py-5">
    <h2 class="mb-3">@ViewData["Title"]</h2>
    <div class="row">
      <div class="col-md-4">
        <form method="post">
          <div asp-validation-summary="ModelOnly" class="text-danger"></div>
          <div class="form-group mb-4 mt-2">
            <label class="form-label" asp-for="Username"></label>
            <input asp-for="Username" class="form-control" aria-required="true" />
            <span asp-validation-for="Username" class="text-red"></span>
          </div>
          <div class="form-group">
            <label class="form-label" asp-for="Email"></label>
            <input asp-for="Email" class="form-control" aria-required="true" />
            <span asp-validation-for="Email" class="text-red"></span>
          </div>
          <button type="submit" class="w-100 btn btn-blue mt-3 rounded-0">@ViewData["Title"]</button>
        </form>
      </div>
    </div>
  </div>
</div>
